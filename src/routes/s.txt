// src/routes/adminRoutes.ts
import { Router } from "express";
import studentManagementRoutes from "./studentRoutes";
import courseRoutes from "./courseRoutes";
import questionRoutes from "./questionRoutes";

const router = Router();

// Toutes ces routes sont déjà protégées en interne par
// authenticateToken + requireAdmin dans leurs fichiers respectifs.
router.use("/students", studentManagementRoutes);
router.use("/courses", courseRoutes);
router.use("/questions", questionRoutes);

export default router;